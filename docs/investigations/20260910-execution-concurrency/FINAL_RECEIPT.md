# 修复回执

## 已完成

- 定位两个任务的轮询响应交替覆盖进度。
- 前端阻止重复提交，校验轮询归属并重试暂时的读取失败。
- 服务端全局拒绝活动任务和清理期间的新执行请求。
- 184 项测试通过，完成隔离浏览器功能、控制台、网络及桌面和窄屏截图验证。
- README 已补充串行执行、HTTP 409、轮询和更新门禁。

## 运行更新门禁

用户要求本轮发布结束后更新运行环境。任务 `1789009629878-4a0eb0f66b0ca8` 与 `1789009702762-1f1116097c9c3` 必须全部退出活动状态后才能更新。当前阶段未取消任务、未强制重启。运行更新结果需另行追加。

## 回滚

若修复存在回归，在所有任务空闲后 revert 修复提交，运行 `npm test`，提交回滚，再通过现有受控空闲重启加载。不得在活动发布期间重启。
