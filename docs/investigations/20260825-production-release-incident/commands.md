# Command Log

以下记录命令类别，省略凭据参数和 Secret 内容。

1. `docker service inspect`、`docker service ps`、`docker ps`、过滤后的 `docker logs`
2. `docker service scale hospital_stack_hospital-backend=1`
3. `docker service update --secret-add ...`
4. `docker service update` 恢复故障前环境集合
5. `docker service update --image hospital-backend:20260825`
6. `docker service update --env-add IMAGE_TAG=20260825`
7. `docker compose config --quiet`
8. `docker stack config`
9. `curl http://127.0.0.1:8190/`
10. 发布器真实 OpenSSH 无破坏传输探针
11. `npm test`
12. `npm run acceptance:full-flow`
13. Live Swarm Secret source and target mapping contract check
