# 菌落结果 UI 门禁验收

目标：提供可执行的业务 UI 验收步骤，避免通用服务健康检查替代玩家弹窗验收。

步骤 `verify-bacteria-result-ui` 已注册，仅目标评估明确选择时生成计划；在后端测试完成后、构建应用镜像前运行状态单元测试和真实证据校验器。证据校验命令失败会阻止后续步骤。

最终意图矩阵：步骤可注册和生成 PASS；顺序位于后端测试之后及镜像构建之前 PASS；执行命令包含状态测试与结果证据校验器 PASS；未知替代步骤拒绝 PASS；无发布器 UI 改动 PASS。

最终 `npm test` 为188项全部通过，0跳过；覆盖有效计划和无效步骤负向案例。本任务未执行生产发布。
