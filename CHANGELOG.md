# 变更记录

## 2026-08-16

1. 修复管理员关系图发布验收在 `DOMContentLoaded` 阶段直接调用 `requestSubmit()` 导致默认 GET 提交和 `/relations?` 循环刷新的误报。初始加载与刷新 pending 门禁改为点击仍处于禁用状态的查询按钮，以浏览器真实控件行为确认不会发出搜索请求；图就绪后的正常邮箱查询继续通过表单提交。医院选择后的场景取证会持续等待所选节点、概览对象、聚焦连线、方向箭头和标签全部同步，避免旧的 ready 快照或重建中间态覆盖有效证据。页面未就绪时会先报告根因并停止无关业务断言，失败日志新增当前 URL、页面就绪状态、关系页文档响应次数、关系图响应次数与状态、网络错误数和运行时错误数等脱敏证据。单元测试补充真实 DOM 时序约束、选择后场景同步约束、刷新阶段禁用控件约束、级联错误收敛和循环刷新证据覆盖。

## 2026-08-10

1. 正式游戏发布计划新增 `verify-relations-release`，复用受控管理员 token、Chrome CDP、WebGL 探针和双前置直连能力，真实验收 `/relations` 页面按邮箱、医院名和院长名查询同一家至少有一条明确关系的医院、查询提交后仍需明确点击结果、搜索在初始加载与刷新完整 pending 窗口保持禁用且阻止提前查询、医院查询接口与图接口的邮箱隔离、唯一原点主节点、全关关系与隐藏孤立节点后的刷新保持，并捕获 HTTP、网络、控制台和运行时错误。关系图载荷同时强制医院 ID 唯一、医院节点标识规范、节点类型仅限医院与公会，拒绝院长、具名角色独立节点及旧身份关系回归。发布探针捕获页面首次关系 API 响应的克隆载荷，并使用驱动页面渲染的同一响应交叉校验初始完整场景，避免 24 小时时间窗自然过期造成独立请求计数漂移；渲染节点、渲染连线、概览逻辑连线和实际几何线段必须与首次响应总数严格一致。明确选择后的聚焦逻辑连线必须等于所选医院关系数，实际精细线对象、方向箭头对象和详情计数分别匹配对应逻辑计数。筛选全关和刷新阶段都要求场景同步，实际关系几何与聚焦对象归零，并保留单个空概览对象和主节点标签。debug 全局句柄不可用时使用 dataset 快照；刷新完成以成功请求周期、控件状态和响应后 dataset 更新序号为准，响应前残留的动画帧与引擎回调不能完成验收。所有阶段的场景证据缺失时失败关闭。实际 Sprite 标签必须匹配预期集合且不超过 `120 + 公会节点数 + 1 个主节点`。正式计划的步骤摘要和验收说明同步覆盖这些门禁，成功和失败契约测试覆盖节点口径、首次 API 与场景总量、自然过期漂移、dataset fallback、请求及快照顺序、各阶段真实场景对象、全量概览线、自适应标签、聚焦连线与箭头、详情计数、三字段查询、邮箱泄露、显式选择、连续加载门禁、主节点位置、刷新保持及浏览器错误门禁。

## 2026-08-07

1. 游戏正式发布在最终运行、双前置静态资源和管理员交易池验收全部通过后，新增服务级历史容器清理。清理只选择 `hospital_stack_hospital-backend` 标记且状态为 `Exited` 的容器，保留当前运行容器、全部镜像和其他服务容器。单个旧容器删除失败记录 `WARNING` 并继续，清理后的目标镜像、Swarm 更新状态、运行副本和健康副本复核继续失败关闭。发布失败、取消、自动回退和 `RECOVERY_REQUIRED` 路径不会进入清理步骤。

## 2026-08-06

1. 数据库 migration 安全门允许同一脚本以 `ADD COLUMN IF NOT EXISTS` 新增的列继续执行 `SET DEFAULT` 与 `SET NOT NULL`，支持幂等回填后的完整约束收口。对既有列的默认值或非空修改，以及删列、改类型、重命名和截断操作继续失败关闭；正反契约测试覆盖允许边界。
2. 论坛发版影响评估支持 `固定数据库影响分类: 详细说明`，发布计划与历史分别保留规范分类和说明，当前中文搜索升级可以正常生成计划。论坛源码门禁加入 `ForumSearchMigrationContractTest`。生产 Compose 修改进入执行边界后，失败、取消或发布器重启统一记录 `RECOVERY_REQUIRED`。游戏数据库分类、迁移必选步骤、切换观察和自动恢复回归测试继续通过。Windows 自动重启测试的临时目录清理重试窗口延长到十秒，覆盖子进程退出后的延迟句柄释放。
3. 论坛初始化脚本语法检查从当前 Git for Windows 安装目录解析 Git Bash，避开 Windows 系统 Bash 与不可用的 WSL 服务入口。论坛源码门禁继续在镜像构建和全部生产动作之前执行。
4. 论坛顶部阶段流程把 `backup-forum-release` 归入“数据安全与迁移”，与实际生产切换前的执行顺序一致。正常论坛发布的回滚证据阶段显示为“记录论坛恢复入口”，避免成功发布时出现“致命故障恢复”变绿的误导。

## 2026-08-05

1. 游戏真实登录验收前置条件新增离线 WebGL 2 framebuffer 能力探针，使用正式 Chrome 与 SwiftShader 参数记录 renderer、WebGL version、viewport、devicePixelRatio、纹理尺寸和 framebuffer 状态。全部 CDP 请求新增独立硬超时，Chrome 启动、调试端口、WebSocket 和请求卡住时可以在限定时间内终止并清理进程与 profile。`Framebuffer Unsupported`、CDP 超时、目标关闭和浏览器进程异常使用全新 profile 隔离复测一次；应用错误、资源错误、网络错误和重复基础设施异常继续失败关闭。真实生产双前置六探针续验通过，Riven 与 VMISS 的冷缓存、暖缓存和 Steam 路径均一次成功。

## 2026-08-04

1. 补齐游戏真实登录验收的 Windows 服务运行配置：正式发布计划从 `release-publisher.config.json` 读取受控 token 文件路径，并作为 `--auth-token-file` 参数显式传给前置检查和发布后验收，消除 Windows 服务未加载用户注册表配置时无法读取用户级环境变量的问题；独立命令继续支持 `RHOSPITAL_RELEASE_AUTH_TOKEN_FILE`。token 前置检查增加游戏签发格式和至少两小时剩余有效期校验，并移动到代码切换与构建之前。缺少、无效或临近到期的受控 token 会在改变工作区版本、耗时构建和全部生产动作前立即停止，日志继续隐藏 token 内容。真实浏览器验收失败时新增页面状态、失败请求 URL、浏览器运行错误和失败响应摘要；New Relic 收集端 `bam.nr-data.net` 的遥测失败单独记录。目标副本全部健康且旧版本运行任务为零后进入新版本观察期，页面立即刷新生产镜像。观察失败时连续三轮复核双前置、发布器直连、生产主机本地认证业务心跳、数据库读写和目标服务；只有全部业务心跳失败且数据库正常才允许自动回退，单点、未知原因、发布器网络异常、数据库故障、取消和服务重启均保留目标版本。Swarm继续使用`pause`。流程总览合并为准备、批量构建、交付、前置资源、数据安全、生产切换、全链路观察和致命恢复八个阶段，恢复步骤只在触发时出现；Docker构建阶段一次执行完整Maven测试和产物编译，后续目标复用缓存。
2. 发布器页面根滚动区固定保留纵向滚动槽，并使用高对比度滚动条。页面内容超过窗口高度时，滚动条会持续提示下方仍有步骤详情或构造历史。

## 2026-08-03

1. 游戏正式发布新增强制前置步骤 `validate-game-static-delivery-prerequisites`，在构建和全部生产动作前检查 Node.js、受控登录 token 文件、Chrome、双前置 IP、网页与 Steam 域名及数值限制。凭据或环境缺失时发布会在镜像上传、前置写入、数据库备份、迁移和 Swarm 切换前停止；检查日志只记录可用性，不输出 token。发布后真实登录验收继续保持失败关闭。
2. Windows 服务宿主改为枚举活动、已连接及断开的 Windows 用户会话，优先选择与安装用户匹配的活动会话，并继续保留物理控制台兜底。发布器运行在 RDP 会话时，Node 受控退出后可以重新取得正确用户令牌并恢复局域网监听。
3. 已完成热滚和最终运行校验、且自动回滚复核明确输出 `HOLD_TARGET` 的健康目标版本，现在会作为下一次发布的生产基线；后置附加验收失败不再让后续计划重复覆盖已经上线的运行文件。计划生成失败时，流程区直接显示具体错误和“尚未执行发布动作”，同时停止所有遗留转圈状态。Windows 自动重启测试的临时目录清理增加占用重试，避免已完成断言被短暂文件句柄占用误报为流程失败。

## 2026-08-02

1. 远程前置演练新增已加载 Nginx/OpenResty `/assets/` 路由检查和现有对象 HTTPS 探针，生产主机演练会在临时目录文件操作通过后确认本地对象根目录、`X-Cache=LOCAL` 与 `X-Asset-Source=gate-object` 已生效；预置动作和静态校验日志同时转发 PASS 标记，并在 HTTP 失败时记录服务端与响应头证据，页面说明同步反映路由检查范围。
2. 生产双前置真实对象验收发现 Nginx `try_files` 直接拼接 HASH 目录会丢失对象键。运维模板改为在 `root` 中使用已校验 HASH 后，发布器远程演练同步检查该路径形式和 `try_files $uri`，并继续以 HTTPS 200、`LOCAL` 与 `gate-object` 作为最终证据。

## 2026-07-31

1. 游戏发布新增内容寻址静态资源交付链。目标提交先构建独立资源清单与对象镜像，再按运维仓库双前置清单向 Riven、VMISS 增量预置并逐文件复算完整 SHA-256；应用切换前逐地址要求 HTTP 200、`X-Cache=LOCAL` 与 `X-Asset-Source=gate-object`，Service Worker 额外要求根作用域许可。任一文件失败时禁止更新 Compose 和执行 Swarm 热滚，旧 HASH 对象保持可读。
2. 游戏发版影响评估新增强制步骤 `verify-game-static-assets-predeploy`，发布计划与单元测试同步覆盖构建、预置、验证和生产动作分类。本次只修改发布器源码与配置读取逻辑，没有执行镜像上传、前置写入或生产发布。
